<% if user_signed_in? %>
  <div class="option" >
     <div class="favorite-btn">
       <p><span>お気に入り登録: </span><%=link_to @combo.favorites.count, combo_favorites_path(@combo.id), method: :post %></p>
     </div>
     <div class="report-btn">
       <p><span>お気に入り解除: </span><%=link_to @combo.favorites.count, combo_favorites_path(@combo.id), method: :delete %></p>
     </div>
     <div class="count-btn">
       <p><span>お気に入りされた数: </span><%= @combo.favorites.count %></p>
     </div>
  </div>
<% else %>
       <p>※※※お気に入りをするにはユーザー登録をしましょう※※※</p>
<% end %>
